@using SharedModelsLib;
@model List<Team>

@{
    ViewData["Title"] = "Liste des teams";
}

@if(Model == null)
{
    <p class="badge bg-warning">Sport introuvable</p>
}
else
{
    
    <component type="typeof(MyBlazorLib.Components.Pages.TeamsComponent)"
               render-mode="ServerPrerendered"
               param-Teams="Model" />
}

